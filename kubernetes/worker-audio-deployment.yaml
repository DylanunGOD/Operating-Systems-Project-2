# ============================================================
# Worker AUDIO — Deployment + Service de métricas (Persona C)
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-audio
  namespace: analysis
  labels: { app: worker-audio, worker: audio }
spec:
  replicas: 2
  selector:
    matchLabels: { app: worker-audio }
  template:
    metadata:
      labels: { app: worker-audio, worker: audio }
    spec:
      containers:
        - name: worker-audio
          image: analysis/worker-audio:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: metrics
              containerPort: 8001
          envFrom:
            - configMapRef: { name: analysis-config }
            - secretRef: { name: analysis-secrets }
          resources:
            requests: { cpu: "500m", memory: "1Gi" }
            limits: { cpu: "2", memory: "4Gi" }
---
apiVersion: v1
kind: Service
metadata:
  name: worker-audio
  namespace: analysis
  labels: { app: worker-audio }
spec:
  selector: { app: worker-audio }
  ports:
    - name: metrics
      port: 8001
      targetPort: 8001
