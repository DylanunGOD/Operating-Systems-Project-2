# ============================================================
# Secret (Persona C) — usa stringData (texto plano en el manifiesto).
# ⚠️ En producción real: NO commitear secretos. Usar Sealed Secrets,
#    External Secrets Operator o el secret manager del cloud.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: analysis-secrets
  namespace: analysis
type: Opaque
stringData:
  SECRET_KEY: "change-me-in-production"
  POSTGRES_PASSWORD: "postgres"
  REDIS_PASSWORD: ""
  GF_SECURITY_ADMIN_PASSWORD: "admin"
