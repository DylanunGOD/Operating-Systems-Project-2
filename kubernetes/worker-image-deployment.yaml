# ============================================================
# Worker IMAGE — Deployment + Service de métricas (Persona C)
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-image
  namespace: analysis
  labels: { app: worker-image, worker: image }
spec:
  replicas: 2
  selector:
    matchLabels: { app: worker-image }
  template:
    metadata:
      labels: { app: worker-image, worker: image }
    spec:
      containers:
        - name: worker-image
          image: analysis/worker-image:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: metrics
              containerPort: 8001
          envFrom:
            - configMapRef: { name: analysis-config }
            - secretRef: { name: analysis-secrets }
          resources:
            requests: { cpu: "500m", memory: "1Gi" }
            limits: { cpu: "2", memory: "4Gi" }
---
apiVersion: v1
kind: Service
metadata:
  name: worker-image
  namespace: analysis
  labels: { app: worker-image }
spec:
  selector: { app: worker-image }
  ports:
    - name: metrics
      port: 8001
      targetPort: 8001
