# Datasource Prometheus provisionado (uid="prometheus", referenciado por los
# dashboards). Montado en /etc/grafana/provisioning/datasources.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
